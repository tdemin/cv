version: "3"

tasks:
  build:
    cmds:
    - |
      latexmk {{.FLAGS}} \
        ru-ops.tex \
        ru-fullstack.tex \
        en-ops.tex \
        en-fullstack.tex
    sources:
    - ./picture.jpg
    - ./**/*.tex
    generates:
    - ./**/*.pdf
    vars:
      FLAGS: -xelatex
